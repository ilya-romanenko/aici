# Дорожная карта деплоя AI Crypto Index

## Этап 0. Аудит и стратегия
- [x] Зафиксировать функциональные требования к лендингу и REST API (эндпоинты, объём трафика, ограничения бесплатного тарифа).
- [x] Проанализировать текущий код фронтенда и Python-пайплайнов, определить зависимости, версии рантаймов и точки интеграции.
- [x] Проверить статические ресурсы, данные и секреты на совместимость с публичным хостингом.
- [x] Сравнить Render и Vercel, выбрать целевой хостинг и архитектуру (SPA + serverless API или единый web service).

## Этап 1. Подготовка репозитория
- [x] Структурировать проект: выделить папки фронтенда, API и общих данных в формате, удобном для выбранного хостинга.
- [x] Обновить описания зависимостей (requirements.txt/poetry, package.json), зафиксировать команды сборки и запуска.
- [x] Настроить .env.example и документацию по переменным окружения, исключить чувствительные данные из git.
- [x] Удалить или адаптировать долгие скрипты/задачи, несовместимые с бесплатными тарифами.

## Этап 2. Фронтенд и ассеты
- [x] Настроить сборку статических ресурсов с минификацией, WebP и lazy-loading изображений.
- [x] Подключить формы и CTA к fetch-запросам на /api/... с обработкой ошибок и подтверждениями.
- [x] Проверить адаптивность, Lighthouse-метрики, SEO-метаданные, Open Graph и фавиконки. (2025-10-21: ensured OG/Twitter meta tags render for smoke tests)
- [x] Подготовить smoke-тесты фронтенда (Playwright или pytest + requests) для критических путей.

## Этап 3. API и данные
- [x] Спроектировать FastAPI (или выбранный фреймворк) с маршрутами /api/health, /api/performance, /api/index-composition и /api/demo-request.
- [x] Организовать загрузку исходных данных (CSV/БД) с учётом ограничений файловой системы/памяти выбранного хостинга.
- [x] Добавить CORS, базовые лимиты, валидацию pydantic и структурированное логирование.
- [x] Написать unit и интеграционные тесты API (pytest + httpx или аналог).

## Этап 4. Контейнеризация и compose
- [x] Подготовить многоэтапный Dockerfile: сборка фронтенда (Node) + рантайм-слой FastAPI на python:3.10-slim, учесть системные зависимости numpy/scipy/tensorflow.
- [x] Добавить .dockerignore и исключить data/, runs/ и прочие артефакты, ожидающиеся как внешние volume.
- [x] Сформировать docker-compose.yml: сервис api, тома для data и runs, .env параметры (`AI_CRYPTO_CONFIG`, `AICI_ENABLE_PIPELINE`, порт), опционально sidecar cloudflared.
- [x] Протестировать локальный `docker compose up`, убедиться что статика и REST работают, описать команды сборки/запуска.

## Этап 5. CI/CD и тестирование
- [x] Настроить GitHub Actions (или аналог) для `docker buildx build` c multi-platform (linux/amd64, linux/arm64) и публикации в Docker Hub.
- [x] Интегрировать pytest/линтеры в pipeline до сборки образа; запускать smoke-тесты контейнера (httpx/pytest) после `docker compose up -d`.
- [x] Сохранять артефакты (asset-manifest, отчёты тестов), публиковать теги образов и уведомления о результате push.

## Этап 6. Запуск и проверка
- [ ] Выполнить первый деплой на сервере (docker compose), смонтировать постоянные каталоги data/ и runs/, залить .env.
- [ ] Подключить cloudflared tunnel, привязать домен, проверить HTTPS/доступность публичных URL (лендинг и /api/...).
- [ ] Провести UAT: CTA/формы, API-маршруты, отдачу ассетов и экспорт run-артефактов.
- [ ] Обновить README/документацию: команды docker-compose, публикация образов, процедура обновления.
- [x] Enable intake form email notifications for demo/API requests.

## Этап 7. Поддержка и развитие
- [x] Регламентировать автоматизацию обновления данных (cron в контейнере/host, ручной триггер, webhooks).
- [ ] Настроить резервное копирование томов (data/, runs/, intake) и процедуру отката образов.
- [ ] Сформировать backlog улучшений: кэширование, мультиязычность, аналитика, оптимизация размера образа.
- [ ] Запланировать регулярные ревью roadmap, обновление версий образов и очистку Docker Hub/сервера.
