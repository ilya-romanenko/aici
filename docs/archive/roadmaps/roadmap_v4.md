## Адаптация интерфейса под устройства

- [x] Реализация мобильного хедера и бургер-тогглера (≤ 959 px)  
  - [x] Обновить `landing.html` и `mini_result.html`: скрывать десктопное меню и показывать кнопку-бургер только когда ширина ≤ 959 px; на больших ширинах возвращать прежний layout без скачков.  
  - [x] Добавить aria-атрибуты, `button`-элемент с фиксированной областью клика ≥ 44 px, фокус-стили и анимацию смены иконки бургер ↔ крестик.  
  - [x] Реализовать off-canvas панель с overlay: фон `rgba(8, 17, 28, 0.6)` + blur, без прокрутки боди (`overflow: hidden`) и с focus-trap для клавиатурной навигации.  
  - [x] Разместить пункты меню колонкой по центру, задать минимальные интервалы 16–20 px, вынести `Get API key`/`Build on Stable` в отдельную кнопку, приклеенную к нижней части панели (full-width, max-width 320 px).  
  - [x] Убрать горизонтальный скролл: проверка `body`, `html`, контейнеров на `overflow-x: hidden`, особое внимание на ширину панели бургер-тогглера.  
  - [x] Акцепт: при открытии меню хедер не сдвигается, фон затемняется, иконка меняется на крестик, скролла по горизонтали нет, кнопка CTA всегда доступна.
  - [x] Fixed the mobile header regression: burger toggle is visible again, drawer stays off-canvas, and header height stays compact on phones.
  - [x] Polished the mobile drawer: removed the sticky blur on phones, widened the sheet to the full viewport, and tucked the CTA directly under the navigation links.
  - [x] Removed the mobile drawer/header gap so the sheet now sits flush with the sticky header when opened.
  - [x] Matched the mobile drawer background color with the header.
  - [x] Защитить мобильную версию от переполнения текста при автопереводе (Google Translate)  
  - [x] Добавить переносы для заголовков и подзаголовков на мобильных ширинах.  
  - [x] Отключить перевод брендового названия и тикеров активов в таблице.
  - [x] Сгладить раскрытие FAQ: отключить анимацию, переключение происходит мгновенно.

- [x] Адаптация таблицы весов в `mini_result.html`  
  - [x] Выбрать подход: до 600 px использовать карточки (stacked) или таблицу с `data-label`; 600–959 px — таблица с горизонтальным скроллом внутри контейнера; ≥ 960 px — текущий вид.  
  - [x] Обновить стили `mini-composition__table` в `main.css`: `display: grid`/`flex` для карточек, чёткие отступы, тени/бордеры, `overflow-x: auto` с маской и инерцией.  
  - [x] Прописать fallback для длинных токенов (ellipsis) и выровнять числовые значения.  
  - [x] Добавить aria-label для скролл-контейнера и визуальный хинт (gradients) о наличии горизонтального скролла на планшетах.  
  - [x] Тест: на 360 px каждая карточка помещается на экран без горизонтального скролла страницы.

- [x] Обновление intake-модалок на мобильных устройствах  
  - [x] Сделать модалки полноэкранными на ширинах ≤ 600 px с крестиком в правом верхнем углу для закрытия.  
  - [x] Центрировать контент, увеличить отступы, адаптировать размеры шрифтов и кнопок для удобства касаний.  
  - [x] Проверить фокус-менеджмент: при открытии фокус на первый интерактивный элемент, при закрытии — возвращение к триггеру.

- [ ] Регрессионные скриншотные тесты  
  - [ ] Настроить Playwright/Percy сценарии для `landing` и `mini_result` на вьюпортах 360×740, 768×1024, 1280×800.  
  - [ ] Смонтировать варианты: меню закрыто, меню открыто, модалка intake открыта, таблица весов на мобильной и планшетной ширинах.  
  - [ ] Подключить прогон к CI (workflow `ci.yml`) и задокументировать шаг по обновлению эталонных снимков.

- [ ] Финальный QA-чеклист перед релизом  
- [ ] Ручной проход по основным сценариям (iOS Safari, Android Chrome, desktop Chrome devtools).  
- [ ] Проверка accessibility: навигация с клавиатуры, screen reader labels, контраст CTA ≥ 4.5:1.  
- [ ] Smoke-тесты ссылок меню и CTA после изменений, подтверждение отсутствия консольных ошибок.  
- [ ] Документирование результатов и фиксируемых тикетов перед деплоем.

- [ ] API усовершенствования  
  - [ ] Вебхуки для событий риска и ребалансировок.  
  - [ ] Allow list для IP, ротация ключей и метрики использования консоли.
  - [ ] SDK и обучающие материалы: Python/TypeScript клиенты и демонстрационные ноутбуки.
  - [ ] Торговые коннекторы для TradingView, MetaTrader и FIX-шлюзов.
